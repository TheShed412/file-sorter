sudo: false
language: cpp
compiler:
  - gcc
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    - catch
    packages:
    - g++-7
script:
  - make mkouts
  - make main
  - make test
  - make run_test